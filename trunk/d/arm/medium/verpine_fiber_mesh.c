inherit ARMOR;
inherit ARMOR_CUSTOM;

void setup()
{
   set_id("Verpine fiber mesh", "fiber mesh", "mesh", "armor");
   set_long("Raxsus Nuli had plans of planetary conquest in the wake of Sith aggression. Though eclipsed by recent events, the Jedi saw the pirate jailed and his Verpine stockpiles auctioned.");

   set_slots("torso", "right arm", "left arm", "right leg", "left leg");

   set_armor_class(8);
   set_mass(15000);
   set_value(2400);
}