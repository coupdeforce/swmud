inherit M_LIGHTSABER_COMPONENT;

void setup()
{
   set_id("Lorrdian Gemstone", "lorrdian", "gemstone", "crystal");
   set_long("During their subjugation, the Lorrdians were banned from speaking to each other and thus learned to use subtle gestures and ticks to communicate.  The few Force-Adepts found after the Jedi freed the population of slaves has imprinted these gemstones with the Force, to enhance the ability to read an opponent.");
   set_component_name("lorrdian gemstone");
   set_component_type("focusing");
   set_armor_bonus(2);
   set_deflection_bonus(6);
   set_mass(50);
   set_value(10000);
}