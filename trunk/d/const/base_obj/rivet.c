inherit OBJ;
inherit M_GETTABLE;
inherit M_VALUABLE;

void setup()
{
   set_id("rivet");
   set_long("A metal rivet used for fastening a blade to a handle or hilt.");

   set_mass(80);
   set_value(100);
}